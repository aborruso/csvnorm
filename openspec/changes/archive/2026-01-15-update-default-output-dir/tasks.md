## 1. Implementation
- [x] 1.1 Update `script/prepare.sh` to default output directory to the current working directory when `--output-dir` is not provided
- [x] 1.2 Update `script/prepare.sh` to stop with a warning when output exists unless `--force` is used
- [x] 1.3 Update documentation to reflect the new default and overwrite behavior (`README.md`, `docs/deployment.md`)
- [x] 1.4 Add/adjust tests or manual smoke instructions to verify default output location and overwrite behavior
